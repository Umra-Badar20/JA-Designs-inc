RewriteEngine On

# Redirect .php to non-.php
RewriteCond %{THE_REQUEST} \s/+(.*)\.php[\s?]
RewriteRule ^ %1 [R=301,L]

# Load .php internally when .php is not present
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^(.+)$ $1.php [L]
